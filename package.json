{
  "name": "mempool_watcher_server",
  "version": "1.0.0",
  "main": "src/mempoolWatcherServer.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/node-ipc": "^9.2.0",
    "@types/ws": "^8.5.3",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/mempoolWatcherServer.js"
  },
  "dependencies": {
    "ethers": "^5.6.2",
    "node-ipc": "^11.1.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.7.0",
    "web3": "^1.7.1",
    "ws": "^8.5.0"
  }
}
